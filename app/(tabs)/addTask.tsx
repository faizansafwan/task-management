import { ScrollView, Text, View } from "react-native";


export default function AddTask() {

    return (
        <ScrollView>
            <View>
                <Text> Add Task</Text>
            </View>
        </ScrollView>
    )
}